module.exports = {
	name: 'ship',
	description: 'Calculates the shipping of two users.',
	args: true,
	execute(Discord, message, args, client) {
		if (args[0] ===  )
			return const rating = Math.floor(Math.random() * 100) + 1;
					message.channel.send(`I ship them ${rating}%!`);

	}
}